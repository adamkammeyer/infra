---
- hosts: all
  become: no
  vars:
  tasks:
    - name: Prepare System
      include_tasks: tasks/system.yml
      tags:
        - system
        - directories
        - flatpak

    - name: Install Software
      include_tasks: tasks/software.yml
      tags:
        - base
        - vscode
